* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

:root {

    --primary: #7654ff;
    --primary-light: #a66cff;

    --purple: #7654ff;
    --blue: #3978ff;
    --green: #35c7a0;
    --orange: #ff8a3d;
    --pink: #f15b88;

    --bg: #f6f7fb;
    --card: #ffffff;

    --text: #161a32;
    --muted: #8b90a5;

    --border: rgba(20, 25, 60, .06);

    --shadow:
        0 15px 40px rgba(34, 28, 75, .07);
}

body.dark {

    --bg: #0c1020;
    --card: #151a2d;

    --text: #ffffff;
    --muted: #9198b2;

    --border: rgba(255,255,255,.06);

    background: var(--bg);
}


/* =========================
   BODY
========================= */

body {

    font-family: "Cairo", sans-serif;

    background:
        radial-gradient(
            circle at 80% 0%,
            rgba(118,84,255,.08),
            transparent 30%
        ),
        var(--bg);

    color: var(--text);

    min-height: 100vh;

    transition:
        background .3s ease,
        color .3s ease;
}


/* =========================
   APP
========================= */

.app {

    display: flex;

    min-height: 100vh;
}


/* =========================
   SIDEBAR
========================= */

.sidebar {

    width: 250px;

    background: var(--card);

    border-left: 1px solid var(--border);

    padding: 28px 18px;

    display: flex;

    flex-direction: column;

    position: fixed;

    right: 0;

    top: 0;

    bottom: 0;

    z-index: 20;
}


.logo {

    display: flex;

    align-items: center;

    gap: 12px;

    padding: 0 10px;

    margin-bottom: 55px;
}


.logo-icon {

    width: 45px;
    height: 45px;

    border-radius: 15px;

    background:
        linear-gradient(
            135deg,
            #7654ff,
            #b15cff
        );

    color: white;

    display: flex;

    justify-content: center;

    align-items: center;

    font-size: 23px;

    font-weight: 900;

    box-shadow:
        0 10px 25px rgba(118,84,255,.28);
}


.logo strong {

    display: block;

    font-size: 18px;

    font-weight: 900;
}


.logo span {

    display: block;

    font-size: 10px;

    color: var(--muted);
}


/* =========================
   NAVIGATION
========================= */

.navigation {

    display: flex;

    flex-direction: column;

    gap: 8px;
}


.nav-item {

    border: 0;

    background: transparent;

    color: var(--muted);

    padding: 14px 16px;

    border-radius: 15px;

    display: flex;

    align-items: center;

    gap: 13px;

    cursor: pointer;

    font-family: inherit;

    font-size: 14px;

    font-weight: 700;

    transition: .25s;
}


.nav-item span {

    width: 25px;

    font-size: 20px;
}


.nav-item:hover {

    background: rgba(118,84,255,.07);

    color: var(--primary);
}


.nav-item.active {

    background:
        linear-gradient(
            90deg,
            rgba(118,84,255,.14),
            rgba(118,84,255,.04)
        );

    color: var(--primary);
}


.sidebar-bottom {

    margin-top: auto;

    padding: 10px;
}


.mini-profile {

    display: flex;

    align-items: center;

    gap: 10px;

    padding: 12px;

    border-radius: 18px;

    background: var(--bg);
}


.avatar {

    width: 42px;
    height: 42px;

    border-radius: 50%;

    display: flex;

    align-items: center;

    justify-content: center;

    background:
        linear-gradient(
            135deg,
            #7654ff,
            #c35cff
        );

    color: white;

    font-weight: 900;
}


.mini-profile strong {

    display: block;

    font-size: 13px;
}


.mini-profile span {

    color: var(--muted);

    font-size: 10px;
}


/* =========================
   MAIN
========================= */

.main {

    width: calc(100% - 250px);

    margin-right: 250px;

    padding: 35px 55px 70px;

    max-width: 1500px;
}


/* =========================
   TOPBAR
========================= */

.topbar {

    display: flex;

    justify-content: space-between;

    align-items: center;

    margin-bottom: 35px;
}


.small-date {

    color: var(--muted);

    font-size: 12px;

    font-weight: 700;

    margin-bottom: 5px;
}


.topbar h1 {

    font-size: 31px;

    font-weight: 900;

    letter-spacing: -.7px;
}


.topbar h1 span {

    font-size: 26px;
}


.subtitle {

    color: var(--muted);

    margin-top: 5px;

    font-size: 13px;
}


.top-actions {

    display: flex;

    align-items: center;

    gap: 12px;
}


.icon-button,
.profile-button {

    width: 48px;
    height: 48px;

    border: 0;

    border-radius: 16px;

    background: var(--card);

    color: var(--text);

    cursor: pointer;

    position: relative;

    box-shadow: var(--shadow);
}


.notification-dot {

    position: absolute;

    width: 7px;
    height: 7px;

    border-radius: 50%;

    background: #ff4e7b;

    top: 10px;
    right: 10px;
}


/* =========================
   PAGE
========================= */

.page {

    display: none;

    animation: pageIn .35s ease;
}


.page.active-page {

    display: block;
}


@keyframes pageIn {

    from {
        opacity: 0;
        transform: translateY(8px);
    }

    to {
        opacity: 1;
        transform: translateY(0);
    }
}


/* =========================
   HERO
========================= */

.hero-card {

    min-height: 235px;

    border-radius: 32px;

    padding: 32px 40px;

    position: relative;

    overflow: hidden;

    color: white;

    background:
        radial-gradient(
            circle at 15% 90%,
            rgba(190,110,255,.65),
            transparent 32%
        ),
        linear-gradient(
            135deg,
            #171b42,
            #493483 60%,
            #8d4fc7
        );

    box-shadow:
        0 25px 50px rgba(76,47,150,.2);
}


.hero-card::before {

    content: "";

    position: absolute;

    width: 320px;
    height: 320px;

    border-radius: 50%;

    background: rgba(255,255,255,.06);

    left: -100px;
    bottom: -200px;
}


.hero-content {

    position: relative;

    z-index: 2;

    max-width: 650px;
}


.hero-badge {

    display: inline-flex;

    padding: 7px 13px;

    border-radius: 30px;

    background: rgba(255,255,255,.12);

    border: 1px solid rgba(255,255,255,.1);

    font-size: 12px;

    margin-bottom: 15px;
}


.hero-card h2 {

    font-size: 28px;

    font-weight: 900;

    margin-bottom: 8px;
}


.hero-card p {

    color: rgba(255,255,255,.72);

    font-size: 13px;

    margin-bottom: 30px;
}


.hero-progress {

    max-width: 600px;
}


.progress-info {

    display: flex;

    justify-content: space-between;

    align-items: center;

    margin-bottom: 9px;

    font-size: 12px;

    font-weight: 700;
}


.progress-info strong {

    font-size: 30px;

    color: #c9aaff;
}


.progress-track {

    height: 9px;

    border-radius: 30px;

    overflow: hidden;

    background: rgba(255,255,255,.12);
}


.progress-fill {

    height: 100%;

    border-radius: inherit;

    background:
        linear-gradient(
            90deg,
            #7654ff,
            #d05cff
        );
}


.hero-decoration {

    position: absolute;

    left: 70px;

    bottom: 35px;

    font-size: 50px;

    opacity: .8;

    animation: float 4s ease-in-out infinite;
}


@keyframes float {

    0%,100% {
        transform: translateY(0);
    }

    50% {
        transform: translateY(-12px);
    }
}


/* =========================
   SECTION HEADER
========================= */

.section-header {

    display: flex;

    align-items: end;

    justify-content: space-between;

    margin: 34px 0 17px;
}


.section-kicker {

    display: block;

    color: var(--primary);

    font-size: 9px;

    font-weight: 900;

    letter-spacing: 1.5px;

    margin-bottom: 3px;
}


.section-header h2 {

    font-size: 20px;

    font-weight: 900;
}


.add-task-small {

    border: 0;

    padding: 10px 15px;

    border-radius: 13px;

    background: rgba(118,84,255,.1);

    color: var(--primary);

    font-family: inherit;

    font-weight: 800;

    cursor: pointer;

    transition: .2s;
}


.add-task-small:hover {

    background: var(--primary);

    color: white;
}


.week-label {

    color: var(--muted);

    font-size: 12px;
}


/* =========================
   TASKS
========================= */

.tasks {

    display: flex;

    flex-direction: column;

    gap: 12px;
}


.task-card {

    display: flex;

    align-items: center;

    gap: 14px;

    background: var(--card);

    border: 1px solid var(--border);

    border-radius: 25px;

    padding: 13px 16px;

    min-height: 105px;

    box-shadow: var(--shadow);

    transition: .25s;
}


.task-card:hover {

    transform: translateY(-2px);

    box-shadow:
        0 20px 40px rgba(34,28,75,.11);
}


.task-number {

    font-size: 11px;

    font-weight: 900;

    color: var(--muted);

    width: 28px;
}


.subject-icon {

    width: 62px;
    height: 62px;

    border-radius: 19px;

    display: flex;

    justify-content: center;

    align-items: center;

    font-size: 27px;

    font-weight: 900;
}


.purple {

    background: rgba(118,84,255,.12);

    color: var(--purple);
}


.blue {

    background: rgba(57,120,255,.11);

    color: var(--blue);
}


.green {

    background: rgba(53,199,160,.12);

    color: var(--green);
}


.orange {

    background: rgba(255,138,61,.12);

    color: var(--orange);
}


.purple-text {
    color: var(--purple);
}

.blue-text {
    color: var(--blue);
}

.green-text {
    color: var(--green);
}


.task-info {

    flex: 1;
}


.subject-name {

    font-size: 11px;

    font-weight: 900;
}


.task-info h3 {

    font-size: 15px;

    margin: 3px 0;

    font-weight: 800;
}


.task-time {

    color: var(--muted);

    font-size: 11px;
}


.start-button {

    border: 0;

    color: white;

    border-radius: 16px;

    padding: 13px 18px;

    min-width: 135px;

    font-family: inherit;

    font-size: 11px;

    font-weight: 900;

    cursor: pointer;

    transition: .2s;
}


.start-button:hover {

    transform: scale(1.03);
}


.purple-button {

    background:
        linear-gradient(
            135deg,
            #7654ff,
            #9857ff
        );
}


.blue-button {

    background:
        linear-gradient(
            135deg,
            #3978ff,
            #5b91ff
        );
}


.completed-button {

    background: rgba(53,199,160,.1);

    color: var(--green);

    cursor: default;
}


.completed {

    opacity: .82;
}


/* =========================
   STREAK
========================= */

.streak-card {

    margin-top: 17px;

    padding: 18px 24px;

    border-radius: 25px;

    background:
        linear-gradient(
            135deg,
            #171c3e,
            #252d5a
        );

    color: white;

    display: flex;

    align-items: center;

    gap: 15px;
}


.streak-icon {

    font-size: 35px;
}


.streak-info {

    flex: 1;
}


.streak-info strong {

    display: block;

    font-size: 18px;

    font-weight: 900;
}


.streak-info span {

    display: block;

    color: rgba(255,255,255,.62);

    font-size: 11px;

    margin-top: 2px;
}


.rocket {

    font-size: 37px;
}


/* =========================
   STATS
========================= */

.statistics-header {

    margin-top: 34px;
}


.stats-grid {

    display: grid;

    grid-template-columns:
        repeat(4,1fr);

    gap: 12px;
}


.stat-card {

    background: var(--card);

    border-radius: 22px;

    padding: 15px;

    min-height: 125px;

    display: flex;

    flex-direction: column;

    box-shadow: var(--shadow);

    border: 1px solid var(--border);
}


.stat-icon {

    width: 36px;
    height: 36px;

    border-radius: 11px;

    display: flex;

    align-items: center;

    justify-content: center;

    margin-bottom: auto;

    font-weight: 900;
}


.purple-bg {
    color: var(--purple);
    background: rgba(118,84,255,.11);
}


.green-bg {
    color: var(--green);
    background: rgba(53,199,160,.11);
}


.orange-bg {
    color: var(--orange);
    background: rgba(255,138,61,.11);
}


.pink-bg {
    color: var(--pink);
    background: rgba(241,91,136,.11);
}


.stat-card strong {

    font-size: 18px;

    font-weight: 900;
}


.stat-card span {

    color: var(--muted);

    font-size: 10px;

    margin-top: 2px;
}


/* =========================
   PAGE TITLE
========================= */

.page-title {

    margin-bottom: 28px;
}


.page-title h2 {

    font-size: 32px;

    font-weight: 900;

    margin: 4px 0;
}


.page-title p {

    color: var(--muted);

    font-size: 13px;
}


/* =========================
   SUBJECTS
========================= */

.subjects-grid {

    display: grid;

    grid-template-columns:
        repeat(2,1fr);

    gap: 17px;
}


.subject-large {

    padding: 25px;

    border-radius: 27px;

    min-height: 230px;

    color: white;

    position: relative;

    overflow: hidden;
}


.purple-card {
    background: linear-gradient(135deg,#6d4dff,#a05aff);
}


.blue-card {
    background: linear-gradient(135deg,#3574ff,#5a9aff);
}


.green-card {
    background: linear-gradient(135deg,#2bb996,#55d5b4);
}


.orange-card {
    background: linear-gradient(135deg,#ff7c31,#ffa154);
}


.subject-large-icon {

    width: 55px;
    height: 55px;

    border-radius: 17px;

    background: rgba(255,255,255,.15);

    display: flex;

    align-items: center;

    justify-content: center;

    font-size: 25px;

    margin-bottom: 20px;
}


.subject-large h3 {

    font-size: 20px;

    font-weight: 900;
}


.subject-large strong {

    position: absolute;

    left: 25px;

    top: 25px;

    font-size: 27px;
}


.subject-large .progress-track {

    margin-top: 25px;

    background: rgba(255,255,255,.18);
}


.subject-large .progress-fill {

    background: white;
}


.subject-large span {

    display: block;

    margin-top: 8px;

    font-size: 11px;

    color: rgba(255,255,255,.75);
}


/* =========================
   PROGRESS
========================= */

.progress-big-card {

    background: var(--card);

    border-radius: 30px;

    padding: 30px;

    box-shadow: var(--shadow);
}


.progress-heading {

    display: flex;

    justify-content: space-between;

    align-items: center;
}


.progress-heading span {

    display: block;

    color: var(--muted);

    font-size: 12px;
}


.progress-heading strong {

    display: block;

    font-size: 40px;

    margin-top: 4px;
}


.growth {

    padding: 9px 13px;

    background: rgba(53,199,160,.12);

    color: var(--green);

    border-radius: 13px;

    font-size: 12px;

    font-weight: 900;
}


.chart {

    height: 300px;

    margin-top: 30px;

    display: flex;

    align-items: end;

    gap: 15px;

    border-bottom: 1px solid var(--border);

    padding: 0 15px;
}


.chart-bar {

    flex: 1;

    max-width: 70px;

    min-height: 20px;

    border-radius: 14px 14px 0 0;

    background: rgba(118,84,255,.13);

    position: relative;

    transition: .3s;
}


.chart-bar:hover {

    background: rgba(118,84,255,.35);
}


.chart-bar.selected {

    background:
        linear-gradient(
            to top,
            #7654ff,
            #b45cff
        );
}


.chart-bar span {

    position: absolute;

    bottom: -30px;

    left: 50%;

    transform: translateX(-50%);

    color: var(--muted);

    font-size: 11px;
}


/* =========================
   SETTINGS
========================= */

.settings-card {

    background: var(--card);

    border-radius: 27px;

    padding: 8px 24px;

    box-shadow: var(--shadow);
}


.setting-row {

    padding: 22px 0;

    display: flex;

    justify-content: space-between;

    align-items: center;

    border-bottom: 1px solid var(--border);
}


.setting-row:last-child {

    border-bottom: 0;
}


.setting-row strong {

    display: block;

    font-size: 14px;
}


.setting-row span {

    color: var(--muted);

    font-size: 11px;
}


/* =========================
   SWITCH
========================= */

.switch {

    position: relative;

    width: 52px;
    height: 29px;
}


.switch input {

    display: none;
}


.slider {

    position: absolute;

    inset: 0;

    border-radius: 50px;

    background: #ddd;

    cursor: pointer;

    transition: .25s;
}


.slider::before {

    content: "";

    position: absolute;

    width: 21px;
    height: 21px;

    top: 4px;
    right: 4px;

    background: white;

    border-radius: 50%;

    transition: .25s;

    box-shadow: 0 2px 6px rgba(0,0,0,.2);
}


.switch input:checked + .slider {

    background: var(--primary);
}


.switch input:checked + .slider::before {

    transform: translateX(-23px);
}


/* =========================
   MODALS
========================= */

.modal-overlay,
.timer-overlay {

    position: fixed;

    inset: 0;

    background: rgba(10,12,28,.62);

    backdrop-filter: blur(10px);

    display: none;

    align-items: center;

    justify-content: center;

    z-index: 100;
}


.modal-overlay.show,
.timer-overlay.show {

    display: flex;
}


.modal {

    width: min(470px, calc(100% - 30px));

    background: var(--card);

    border-radius: 30px;

    padding: 30px;

    position: relative;

    box-shadow:
        0 30px 80px rgba(0,0,0,.25);

    animation: modalIn .25s ease;
}


@keyframes modalIn {

    from {
        transform: scale(.95) translateY(10px);
        opacity: 0;
    }

    to {
        transform: scale(1) translateY(0);
        opacity: 1;
    }
}


.modal-close {

    position: absolute;

    top: 15px;
    left: 15px;

    width: 36px;
    height: 36px;

    border: 0;

    border-radius: 12px;

    background: var(--bg);

    color: var(--text);

    cursor: pointer;

    font-size: 20px;
}


.modal-icon {

    width: 55px;
    height: 55px;

    display: flex;

    align-items: center;

    justify-content: center;

    background: rgba(118,84,255,.11);

    border-radius: 17px;

    font-size: 25px;

    margin-bottom: 15px;
}


.modal h2 {

    font-size: 24px;

    font-weight: 900;
}


.modal > p {

    color: var(--muted);

    font-size: 12px;

    margin: 4px 0 20px;
}


.modal form {

    display: flex;

    flex-direction: column;

    gap: 8px;
}


.modal label {

    font-size: 11px;

    font-weight: 800;

    margin-top: 6px;
}


.modal input,
.modal select {

    width: 100%;

    border: 1px solid var(--border);

    background: var(--bg);

    color: var(--text);

    border-radius: 15px;

    padding: 13px;

    font-family: inherit;

    outline: none;
}


.modal input:focus,
.modal select:focus {

    border-color: var(--primary);
}


.submit-button {

    margin-top: 10px;

    border: 0;

    border-radius: 17px;

    padding: 15px;

    background:
        linear-gradient(
            135deg,
            #7654ff,
            #a65aff
        );

    color: white;

    font-family: inherit;

    font-weight: 900;

    cursor: pointer;
}


/* =========================
   TIMER
========================= */

.timer-modal {

    width: min(520px, calc(100% - 30px));

    background: var(--card);

    border-radius: 35px;

    padding: 35px;

    text-align: center;

    position: relative;
}


.timer-label {

    color: var(--primary);

    font-size: 12px;

    font-weight: 900;
}


.timer-modal h2 {

    font-size: 24px;

    margin-top: 5px;
}


.timer-circle {

    width: 280px;
    height: 280px;

    margin: 30px auto;

    border-radius: 50%;

    background:
        conic-gradient(
            #7654ff 0deg,
            #b65cff 150deg,
            rgba(118,84,255,.1) 150deg
        );

    display: flex;

    align-items: center;

    justify-content: center;
}


.timer-circle::before {

    content: "";

    position: absolute;
}


.timer-circle > div {

    width: 235px;
    height: 235px;

    background: var(--card);

    border-radius: 50%;

    display: flex;

    flex-direction: column;

    justify-content: center;

    align-items: center;
}


.timer-circle strong {

    font-size: 50px;

    font-weight: 900;

    letter-spacing: 2px;
}


.timer-circle span {

    color: var(--muted);

    font-size: 12px;
}


.timer-buttons {

    display: flex;

    gap: 10px;
}


.timer-start,
.timer-finish {

    border: 0;

    border-radius: 17px;

    padding: 15px;

    font-family: inherit;

    font-weight: 900;

    cursor: pointer;
}


.timer-start {

    flex: 1;

    color: white;

    background: linear-gradient(
        135deg,
        #7654ff,
        #a65aff
    );
}


.timer-finish {

    background: var(--bg);

    color: var(--text);

    padding-inline: 20px;
}


/* =========================
   MOBILE
========================= */

@media (max-width: 950px) {

    .sidebar {

        width: 80px;

        padding: 25px 10px;
    }

    .logo {

        justify-content: center;
    }

    .logo > div:last-child {

        display: none;
    }

    .nav-item {

        justify-content: center;

        padding: 14px;
    }

    .nav-item label {

        display: none;
    }

    .mini-profile {

        justify-content: center;
    }

    .mini-profile > div:last-child {

        display: none;
    }

    .main {

        width: calc(100% - 80px);

        margin-right: 80px;

        padding: 30px;
    }

    .stats-grid {

        grid-template-columns:
            repeat(2,1fr);
    }
}


@media (max-width: 650px) {

    .sidebar {

        width: 100%;

        height: 72px;

        top: auto;

        bottom: 0;

        right: 0;

        padding: 6px 12px;

        border-top: 1px solid var(--border);

        border-left: 0;
    }

    .logo,
    .sidebar-bottom {

        display: none;
    }

    .navigation {

        width: 100%;

        flex-direction: row;

        justify-content: space-around;
    }

    .nav-item {

        flex: 1;

        flex-direction: column;

        gap: 2px;

        padding: 5px;

        font-size: 9px;
    }

    .nav-item label {

        display: block;
    }

    .nav-item span {

        font-size: 18px;
    }

    .main {

        width: 100%;

        margin-right: 0;

        padding: 20px 15px 100px;
    }

    .topbar h1 {

        font-size: 23px;
    }

    .hero-card {

        padding: 25px;

        min-height: 220px;
    }

    .hero-card h2 {

        font-size: 23px;
    }

    .hero-decoration {

        left: 20px;

        bottom: 15px;
    }

    .section-header {

        align-items: center;
    }

    .task-card {

        padding: 11px;

        gap: 9px;
    }

    .task-number {

        display: none;
    }

    .subject-icon {

        width: 52px;
        height: 52px;

        font-size: 21px;
    }

    .task-info h3 {

        font-size: 13px;
    }

    .start-button {

        min-width: 90px;

        padding: 11px 8px;

        font-size: 9px;
    }

    .streak-card {

        padding: 15px;
    }

    .rocket {

        display: none;
    }

    .stats-grid {

        grid-template-columns:
            repeat(2,1fr);
    }

    .subjects-grid {

        grid-template-columns: 1fr;
    }

    .page-title h2 {

        font-size: 27px;
    }

    .chart {

        gap: 6px;
    }

    .timer-circle {

        width: 240px;
        height: 240px;
    }

    .timer-circle > div {

        width: 200px;
        height: 200px;
    }

    .timer-circle strong {

        font-size: 42px;
    }
}
